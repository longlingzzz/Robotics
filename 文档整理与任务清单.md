# 微处理器与机器人 — 文档整理与任务清单

> 生成时间：2026-09-15。基于 `文档/` 目录下 9 份资料与 `Project001/` 实验报告工程整理。

---

## 一、当前需要完成的任务

**课程：** 微处理器与机器人（深圳大学 计算机与软件学院，指导教师：王东升）
**实验项目：** 跑马灯点亮实验（报告工程：`Project001/`，LaTeX 深大实验报告模板）

### 1. 实验本体（编程 + 上板验证）

按 `Project001/main.tex` 中已写好的"实验目的与要求"：

| # | 任务 | 说明 |
|---|------|------|
| 1 | 8 个 LED 实现 **4 种明显差异的跑马灯特效** | 如顺序流水、来回扫描、间隔跳亮、累进点亮等 |
| 2 | **4 个按键**分别选择对应特效 | KEY0~KEY3，每个按键切换一种特效 |
| 3 | 蜂鸣器提示音 | 每次**有效**按键按下时只鸣响**一次**，不能长按连响；灯效稳定切换 |
| 4 | （选做）PWM 呼吸灯 | 用定时器 PWM 占空比渐变实现平滑亮度变化 |
| 5 | 工程质量要求 | 模块化划分（led / key / beep 等）、变量与注释含义明确、外设互不干扰 |

### 2. 实验报告（`Project001/`，XeLaTeX 编译）

- `main.tex` 封面**学生信息还没填**：专业、姓名、学号、班级、实验时间、提交时间（见 main.tex 第 26~31 行）。
- 正文五段中，**"一、实验目的与要求"已写好**，还剩四段空着：
  - **二、方法、步骤** — 算法原理、程序设计思路（可用流程图）、数据结构、函数调用关系
  - **三、实验过程及内容** — 代码说明与分析（要求排版整齐、可读性高）
  - **四、实验结论** — 运行结果 + 探讨分析 + 改进想法（**需要放运行截图**）
  - **五、实验体会** — 自行填写
- 流程图、运行截图放入 `Project001/images/`。
- 编译：`latexmk -xelatex main.tex` 或 `make`（需要 MacTeX/TeX Live）。

---

## 二、资料清单与用途速查

| 文件 | 规模 | 用途 |
|------|------|------|
| 面包板实验手册 V1.0.pdf | 36 页 | **动手实验的主教材**：6 个面包板基础实验（GPIO 输出/输入、PWM、ADC），含接线、驱动代码、HAL 函数讲解 |
| STM32F429 阿波罗开发指南_V1.0.pdf | 1329 页 | **开发板主教程**：环境搭建、新建 HAL 工程、跑马灯/按键/定时器等全部实验详解 |
| 阿波罗V2 硬件参考手册_V1.0（F429版本）.pdf | 51 页 | 板载硬件资源详解：LED、按键、蜂鸣器、供电、IO 分配（1.5 节） |
| 阿波罗V2 STM32F429开发板IO引脚分配表.xlsx | 145 行 | **每个 IO 接了什么、能否独立使用**；选引脚接面包板时必查 |
| STM32F4xx参考手册_V4(中文版).pdf | 1284 页 | 寄存器级权威参考（查 GPIO/TIM 外设寄存器细节时用） |
| STM32F429_CORE_V2.pdf | 6 页 | 核心板原理图 |
| 阿波罗STM32F429核心板引脚引出图.pdf | 2 页 | 引脚引出位置图（面包板接线时对照） |
| DNF429_F767_H743-EB嵌入式实验箱用户手册 V1.0.pdf | 17 页 | 实验箱组成、面包板供电方式、资料包位置 |
| DP100数控电源用户手册 V1.2.pdf | 17 页 | 数控电源操作（给开发板/面包板供电用） |

---

## 三、关键表格整理

### 1. 阿波罗 V2（F429）板载关键外设引脚 —— 本实验核心

| 外设 | 引脚 | 有效电平 / 说明 |
|------|------|----------------|
| LED0（DS0，红色） | **PB1** | 低电平点亮（IO 经限流电阻接 VCC） |
| LED1（DS1，绿色） | **PB0** | 低电平点亮 |
| KEY0 | **PH3** | 按下为低电平（内部上拉） |
| KEY1 | **PH2** | 按下为低电平（内部上拉） |
| KEY2 | **PC13** | 按下为低电平（内部上拉） |
| KEY_UP（WK_UP） | **PA0** | **高电平触发**（按下为高），兼待机唤醒脚 |
| 有源蜂鸣器 BEEP | **PCF8574 的 P0 引脚** | ⚠️ 不直接接 GPIO！需通过 **IIC（SCL=PH4 / SDA=PH5）** 写 PCF8574 控制鸣响，高电平鸣响 |

> ⚠️ **资源缺口提醒：** 板载可编程 LED 只有 **2 个**，而实验要求 **8 个 LED**。需要用实验箱面包板外接直插 LED（参考面包板手册的接法：LED 负极接 MCU 引脚、正极经 1KΩ 限流电阻接 VCC → 低电平点亮；或自行决定接法并一致处理）。额外 6 个 IO 引脚请对照 IO 分配表选"完全独立"的引脚（如 PB6、PB5、PA4、PA8~PA12、PC6/PC7 等，优先选未插 LCD/模块时完全独立的）。4 个按键正好可用板载 KEY0/KEY1/KEY2/KEY_UP。

### 2. 面包板实验手册 6 个实验（基于 STM32F103C8T6 小系统板）

| 章 | 实验 | 引脚 | 要点 |
|----|------|------|------|
| 1 | LED 闪烁 | LED0–PA8 | GPIO 推挽输出；LED 低电平点亮；HAL_GPIO_WritePin/TogglePin |
| 2 | LED 跑马灯 | LED0~3 = PA8/PA9/PA10/PA11 | 顺序点亮/熄灭，流水灯；宏定义封装引脚 |
| 3 | 单按键控制 LED | KEY0–PB0，LED0–PA8 | GPIO 输入；机械按键抖动 → 延时消抖 |
| 4 | 多按键控制 LED | KEY0~3 = PB0/PA7/PA6/PA5；LED0~3 = PA8~PA11 | 4 路输入判断，按键-LED 一一对应 |
| 5 | PWM 控制 LED | TIM1 CH1~4 = PA8~PA11 | ⚠️ 必须注销串口初始化（PA9/PA10 复用为 USART1） |
| 6 | 可调亮度 LED | ADC1_IN1 = PA1 接 5KΩ 电位器 | ADC 采样值作 TIM1 比较值 → 亮度随电位器变化 |

> 注：该手册是为实验箱**面包板上的 F103 小系统板**编写的；本课程报告用 F429 开发板时，**方法可照搬，引脚要按上面第 1 张表 / IO 分配表替换**。

### 3. 阿波罗开发指南 — 本实验相关章节速查

| 章节 | 页码 | 内容 |
|------|------|------|
| 第三章 开发环境搭建 | p48 | MDK、仿真器、CH340 串口驱动安装 |
| 第四章 STM32 初体验 | p51 | 编译例程、串口下载、DAP 下载与调试 |
| 第八章 新建 HAL 版本 MDK 工程 | p129 | 从零建 HAL 工程流程 |
| 10.3 STM32CubeMX 新建工程 | p167 | CubeMX 建工程（生成 HAL 初始化代码） |
| **第十三章 跑马灯实验** | p203 | GPIO 输出、LED 驱动写法 |
| **第十四章 按键输入实验** | p219 | 独立按键、消抖、读取 |
| 第二十章 通用定时器实验（20.3 PWM 输出 p293） | p280 | PWM 频率/占空比 → 呼吸灯、蜂鸣器思路 |
| 第三十六章 IIC 实验 | p626 | ⚠️ 读 PCF8574 控制蜂鸣器必需 |
| 第三十七章 IO 扩展实验 | p644 | PCF8574 芯片驱动（BEEP 所挂芯片） |

### 4. 实验箱面包板供电（DNF429 手册）

- **方式一（推荐）**：打开实验箱底板"面包板电源开关"→ 面包板 3.3V/5V 直接从开发板取电。先确认 4 个电源指示灯亮。
- **方式二**：开关打到 OFF，用 DP100 经鳄鱼夹接 **P2 端子（5V）** 或 **P3 端子（3.3V）** 供电。
- ⚠️ 用 DP100 时**先设好电压再输出**，正负极不可接反。
- 面包板规则：**窄条竖向导通**（供电用），**宽条横向导通、左右不通**（信号用）。

---

## 四、建议的实施顺序

1. **建工程**：用 STM32CubeMX/MDK 建 F429 HAL 工程（参考开发指南第八/十章），配置 LED×8、KEY×4。
2. **LED 驱动**：先点亮 1 个 → 8 个顺序流水（面包板手册第 1、2 章 + 开发指南第十三章）。
3. **按键驱动**：消抖 + 边沿检测（区分"按下事件"与"持续按下"），KEY0~KEY3 切换 4 种特效（第 3、4 章 + 第十四章）。
4. **蜂鸣器**：IIC 驱动 PCF8574 → P0 输出高 → 鸣响固定时长（如 50~100ms）后自动关闭，避免长按连响（第三十六/三十七章）。
5. **选做呼吸灯**：定时器 PWM，占空比 0→100→0% 渐变（第二十章）。
6. **截图 + 写报告**：每种特效拍运行照片，填 `Project001/main.tex` 四个空段落和学生信息，`latexmk -xelatex` 编译出 PDF。
